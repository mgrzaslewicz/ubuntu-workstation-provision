---
- name: Install spotify
  hosts: all
  gather_facts: False
  tags: snap,graphical,media,spotify
  tasks:
    - name: Install spotify
      become: true
      community.general.snap:
        name: spotify
